<div *ngIf="monthlyLogs" class="card">
  <div class="table-head p-4 d-flex align-items-center justify-content-between">
    <div class="d-flex align-items-center">
      <i class="arrow i-Arrow-Left-in-Circle" (click)="prevMonth()"></i>
      <div class="month m-2">
        <strong>{{ currMonthLog.month | date: "MMMM yyyy" }}</strong>
      </div>
      <i class="arrow i-Arrow-Right-in-Circle" (click)="nextMonth()"></i>
    </div>
    <button type="button" class="btn btn-primary m-1">Save</button>
  </div>

  <div *ngIf="days" class="table-body p-4">
    <div class="table-body-head d-flex align-items-center">
      <span class="col">Date</span>
      <span class="col">Select activity and hours</span>
      <span class="col">SmartWorking</span>
    </div>
    <hr />

    <div *ngFor="let day of days; let j = index">
      <!-- date -->
      <div class="d-flex align-items-center">
        <span class="col"
          ><strong>{{ day.date | date: "d EEEE" }}</strong></span
        >
        <!-- activities and hours -->
        <div *ngIf="day.activity_days_array[0]" [formGroup]="activitiesForm">
          <div formArrayName="activitiesArray" class="col">
            <!--show old activities -->
            <div
              *ngFor="let activity of day.activity_days_array"
              class="d-flex"
            >
              <select class="form-control mb-2 w-50">
                <option selected>{{ activity.activity_name }}</option>
                <option
                  *ngFor="let activity of activities"
                  [value]="activity.id"
                >
                  {{ activity.name }}
                </option>
              </select>
              <div class="col" *ngIf="day.activity_days_array[0]">
                <input
                  type="number"
                  name="hours_spent"
                  id="hours_spent"
                  min="0"
                  max="8"
                  value="{{ activity.hours_spent }}"
                  class="form-control mb-2 w-50"
                />
              </div>
            </div>
            <!-- create new activities -->
            <div
              *ngFor="
                let activityForm of activitiesArray.controls;
                let i = index
              "
            >
              <div class="d-flex">
                <!-- activity input -->
                <select class="form-control mb-2 w-50">
                  <option
                    *ngFor="let activity of activities"
                    [value]="activity.id"
                  ></option>
                </select>
                <!-- hours input -->
                <div class="col">
                  <input
                    type="number"
                    name="hours_spent"
                    id="hours_spent"
                    min="0"
                    max="8"
                    class="form-control mb-2 w-50"
                  />
                </div>
              </div>
            </div>
            <button
              (click)="addActivity()"
              type="button"
              class="btn btn-sm btn-outline-primary"
            >
              Add
            </button>
          </div>
        </div>

        <!-- smartworking -->
        <div class="col mb-3" *ngIf="day.activity_days_array[0]">
          <label class="checkbox checkbox-primary">
            <input type="checkbox" [checked]="day.smartworking == 1" />
            <span class="checkmark"></span>
          </label>
        </div>
      </div>
      <hr />
    </div>
  </div>
  <div *ngIf="!days" class="loader-bubble loader-bubble-primary m-5"></div>
</div>
<div *ngIf="!monthlyLogs" class="loader-bubble loader-bubble-primary m-5"></div>





<div class="d-flex align-items-center">
  <span class="col"
    ><strong>{{ day.date | date: "d EEEE" }}</strong></span
  >
  <!-- activities and hours -->
  <div class="col" [formGroup]="activitiesForm">
    <div formArrayName="activitiesArray">
      <!--show old activities -->
      <div *ngFor="let activity of day.activity_days_array" class="d-flex">
        <select class="form-control mb-2">
          <option selected>{{ activity.activity_name }}</option>
          <option *ngFor="let activity of activities" [value]="activity.id">
            {{ activity.name }}
          </option>
        </select>

        <input
          type="number"
          name="hours_spent"
          id="hours_spent"
          min="0"
          max="8"
          value="{{ activity.hours_spent }}"
          class="form-control mb-2 ml-2 w-50"
        />
      </div>
      <!-- create new activities -->
      <div *ngFor="let activityForm of activitiesArray.controls; let i = index">
        <div class="d-flex">
          <!-- activity input -->
          <select class="form-control mb-2">
            <option *ngFor="let activity of activities" [value]="activity.id">
              {{ activity.name }}
            </option>
          </select>
          <!-- hours input -->

          <input
            type="number"
            name="hours_spent"
            id="hours_spent"
            min="0"
            max="8"
            class="form-control mb-2 ml-2 w-50"
          />
        </div>
      </div>
      <button
        (click)="addActivity()"
        type="button"
        class="btn btn-sm btn-outline-primary"
      >
        Add
      </button>
    </div>
  </div>

  <!-- smartworking -->
  <div class="col mb-3">
    <label class="checkbox checkbox-primary">
      <input type="checkbox" [checked]="day.smartworking == 1" />
      <span class="checkmark"></span>
    </label>
  </div>

  <div class="col mb-1">
    <btn-loading
      *ngFor="let btn of loadingButtons"
      btnClass="btn-{{ btn.name }}  btn-sm "
      [loading]="btn.loading"
      loadingText="Saving..."
      (click)="showLoading(btn)"
    >
      Save
    </btn-loading>
  </div>
</div>
<hr />
